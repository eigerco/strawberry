[package]
name = "bandersnatch"
version = "0.1.0"
edition = "2021"

[dependencies]
sp-core = { git="https://github.com/paritytech/polkadot-sdk", features = ["full_crypto", "bandersnatch-experimental"] }

[lib]
crate-type = ["cdylib"]